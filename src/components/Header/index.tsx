import React from 'react';
import * as Styled from './styles';

const Header: React.FC = () => {
  return (
    <Styled.Container>
      <p>header</p>
    </Styled.Container>
  );
};

export default Header;
