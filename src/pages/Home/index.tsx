import React from 'react';
import { Button } from 'primereact/button';

const Home: React.FC = () => {
  return (
    <div>
      <Button className="p-button-primary" color="primary">
        Home
      </Button>
    </div>
  );
};

export default Home;
